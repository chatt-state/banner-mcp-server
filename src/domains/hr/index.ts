/**
 * HR domain module (placeholder).
 *
 * Will be implemented in Phase 4 with employees, positions,
 * jobs, payroll, etc.
 */

import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';

/** Registers HR domain tools with the MCP server. */
export function register(_server: McpServer): void {
  // Phase 4: HR tools
}
