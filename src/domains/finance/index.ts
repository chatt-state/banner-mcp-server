/**
 * Finance domain module (placeholder).
 *
 * Will be implemented in Phase 4 with accounting-strings,
 * ledger-activities, budgets, AP/AR, etc.
 */

import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';

/** Registers finance domain tools with the MCP server. */
export function register(_server: McpServer): void {
  // Phase 4: finance tools
}
